window.addEventListener('DOMContentLoaded', function() {
    const cardNavItem = document.getElementById('card');
    cardNavItem.classList.add('active');
    const homeNavItem = document.getElementById('home');
    homeNavItem.classList.remove('active');
  });